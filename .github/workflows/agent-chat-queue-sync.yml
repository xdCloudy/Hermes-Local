name: Agent chat queue sync

on:
  push:
    branches:
      - agent/chat-queue-a4
    paths:
      - "agent-patches/chat-queue-sync.py"

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0
      - run: python agent-patches/chat-queue-sync.py
      - run: cargo fmt --all
      - run: git diff --check
      - shell: bash
        run: |
          rm -f agent-patches/chat-queue-sync.py
          rm -f .github/workflows/agent-chat-queue-sync.yml
      - shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: sync stacked chat extraction cleanup"
          git push origin HEAD:${{ github.ref_name }}
