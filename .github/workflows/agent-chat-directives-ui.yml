name: Agent chat directives UI

on:
  push:
    branches:
      - agent/chat-composer-a4
    paths:
      - "agent-patches/chat-directives-ui.py"

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.ref_name }}
          fetch-depth: 0
      - run: python agent-patches/chat-directives-ui.py
      - run: cargo fmt --all
      - run: git diff --check
      - shell: bash
        run: |
          rm -f agent-patches/chat-directives-ui.py
          rm -f .github/workflows/agent-chat-directives-ui.yml
      - shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: execute slash directives through typed session service"
          git push origin HEAD:${{ github.ref_name }}
