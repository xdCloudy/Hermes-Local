From 091bdd71d5c6bb4a14816ee3f18ec0edf6cb8ede Mon Sep 17 00:00:00 2001
From: xdCloudy <connerabery@gmail.com>
Date: Thu, 30 Jul 2026 04:47:48 +0100
Subject: [PATCH] fix(desktop): keep packaged chats projectless

---
 .../e2e/hermes-local-functional.spec.ts       |  51 ++++++++
 .../composer/status-stack/coding-row.test.tsx |  40 +++++++
 .../chat/composer/status-stack/coding-row.tsx |  20 +++-
 .../src/app/gateway/hooks/use-gateway-boot.ts |  16 +--
 .../hooks/use-session-actions.test.tsx        |   4 +-
 apps/desktop/src/store/projects.test.ts       |  17 ++-
 apps/desktop/src/store/projects.ts            |  20 +---
 apps/desktop/src/store/session.test.ts        |  29 ++++-
 apps/desktop/src/store/session.ts             |  34 +++---
 apps/desktop/src/store/updates.test.ts        |   6 +-
 apps/desktop/src/store/updates.ts             |   3 +-
 tests/test_tui_gateway_server.py              | 113 +++++++++++++++++-
 tests/tui_gateway/test_session_cwd_follow.py  |   7 +-
 tui_gateway/methods_session.py                |  23 ++--
 tui_gateway/server.py                         |  35 ++++--
 15 files changed, 344 insertions(+), 74 deletions(-)
 create mode 100644 apps/desktop/src/app/chat/composer/status-stack/coding-row.test.tsx

diff --git a/apps/desktop/e2e/hermes-local-functional.spec.ts b/apps/desktop/e2e/hermes-local-functional.spec.ts
index f4edde2ef..84b50002d 100644
--- a/apps/desktop/e2e/hermes-local-functional.spec.ts
+++ b/apps/desktop/e2e/hermes-local-functional.spec.ts
@@ -35,6 +35,20 @@ interface LoginItemWindow {
   }
 }
 
+interface LocalActionTask {
+  id: string
+  status: 'failed' | 'running' | 'succeeded'
+}
+
+interface LocalActionWindow {
+  hermesDesktop: {
+    localWorkstation: {
+      actionStatus: (taskId: string) => Promise<LocalActionTask>
+      startAction: (action: 'restart') => Promise<LocalActionTask>
+    }
+  }
+}
+
 async function workstationState(page: Page): Promise<WorkstationState> {
   return page.evaluate(() =>
     (
@@ -49,6 +63,14 @@ async function workstationState(page: Page): Promise<WorkstationState> {
   )
 }
 
+async function expectProjectlessComposer(page: Page): Promise<void> {
+  const codingRow = page.locator('[data-slot="composer-surface"] .coding-status-bar')
+
+  await expect(codingRow.getByText('No project', { exact: true })).toBeVisible({ timeout: 60_000 })
+  await expect(codingRow.getByText('main', { exact: true })).toHaveCount(0)
+  await expect(codingRow.locator('button')).toHaveCount(0)
+}
+
 test.describe('Hermes Local packaged functional acceptance', () => {
   test.skip(!ENABLED, 'Set HERMES_LOCAL_FUNCTIONAL_ACCEPTANCE=1 to test the packaged local workstation.')
 
@@ -136,6 +158,35 @@ test.describe('Hermes Local packaged functional acceptance', () => {
         health: { dashboard: true, hermes: true, model: true },
         runtime: { controllerAlive: true }
       })
+
+      // The workstation's execution directory lives below ROOT, which itself
+      // is a Git checkout. It must remain an internal process detail: a fresh
+      // chat starts projectless and stays that way through both gateway
+      // reconnect and renderer reload.
+      await page.getByRole('button', { name: 'Open Chat', exact: true }).click()
+      await expectProjectlessComposer(page)
+
+      const restartTask = await page.evaluate(() =>
+        (window as unknown as LocalActionWindow).hermesDesktop.localWorkstation.startAction('restart')
+      )
+
+      await expect
+        .poll(
+          () =>
+            page.evaluate(
+              taskId =>
+                (window as unknown as LocalActionWindow).hermesDesktop.localWorkstation.actionStatus(taskId),
+              restartTask.id
+            ),
+          { timeout: 120_000 }
+        )
+        .toMatchObject({ status: 'succeeded' })
+      await expectProjectlessComposer(page)
+
+      await page.reload()
+      await expectProjectlessComposer(page)
+
+      await nav.getByRole('button', { exact: true, name: 'Home' }).click({ timeout: 60_000 })
       await page.getByRole('button', { name: 'Refresh workstation' }).click()
       await expect(page.getByRole('button', { name: 'Refresh workstation' })).toBeEnabled({ timeout: 30_000 })
       await page.screenshot({ fullPage: true, path: path.join(evidence, 'packaged-home.png') })
diff --git a/apps/desktop/src/app/chat/composer/status-stack/coding-row.test.tsx b/apps/desktop/src/app/chat/composer/status-stack/coding-row.test.tsx
new file mode 100644
index 000000000..c92182510
--- /dev/null
+++ b/apps/desktop/src/app/chat/composer/status-stack/coding-row.test.tsx
@@ -0,0 +1,40 @@
+import { cleanup, render, screen } from '@testing-library/react'
+import { afterEach, describe, expect, it, vi } from 'vitest'
+
+import { $repoStatus } from '@/store/coding-status'
+
+import { CodingStatusRow } from './coding-row'
+
+vi.mock('@/i18n', () => ({
+  useI18n: () => ({
+    t: {
+      rightSidebar: { noProjectTitle: 'No project' },
+      sidebar: { projects: {} },
+      statusStack: { coding: {} }
+    }
+  })
+}))
+
+describe('CodingStatusRow projectless state', () => {
+  afterEach(() => {
+    cleanup()
+    $repoStatus.set(null)
+  })
+
+  it('shows No project when the chat has no attached workspace', () => {
+    $repoStatus.set(null)
+
+    render(<CodingStatusRow repoPath="" />)
+
+    expect(screen.getByText('No project')).not.toBeNull()
+    expect(screen.queryByText('main')).toBeNull()
+  })
+
+  it('does not mislabel an explicitly attached non-Git folder as projectless', () => {
+    $repoStatus.set(null)
+
+    render(<CodingStatusRow repoPath="/selected/folder" />)
+
+    expect(screen.queryByText('No project')).toBeNull()
+  })
+})
diff --git a/apps/desktop/src/app/chat/composer/status-stack/coding-row.tsx b/apps/desktop/src/app/chat/composer/status-stack/coding-row.tsx
index 14cb89d3d..a8d587a0f 100644
--- a/apps/desktop/src/app/chat/composer/status-stack/coding-row.tsx
+++ b/apps/desktop/src/app/chat/composer/status-stack/coding-row.tsx
@@ -22,6 +22,9 @@ import { $newWorktreeRequest } from '@/store/projects'
 // Tiny uppercase section header, matching the composer "+" menu's labels.
 const MENU_SECTION = 'text-[0.625rem] font-semibold uppercase tracking-wider text-(--ui-text-tertiary)'
 
+const CODING_ROW_CLASS =
+  'coding-status-bar min-h-7 rounded-t-[inherit] rounded-b-none border-b border-(--ui-stroke-tertiary) px-3.5 py-1.5 hover:bg-transparent'
+
 interface CodingStatusRowProps {
   /** Branch the current draft off into a fresh worktree + session, based on
    *  `base` (a branch name; omitted = current HEAD). The composer owns the
@@ -112,10 +115,23 @@ export const CodingStatusRow = memo(function CodingStatusRow({
     setTimeout(() => setWorktreeOpen(true), 0)
   }
 
-  if (!status) {
+  if (!status && resolvedRepoPath) {
     return null
   }
 
+  if (!status) {
+    return (
+      <StatusRow
+        className={CODING_ROW_CLASS}
+        leading={<Codicon className="text-muted-foreground/75" name="folder" size="0.8rem" />}
+      >
+        <span className="min-w-0 truncate text-xs font-normal text-muted-foreground/92">
+          {t.rightSidebar.noProjectTitle}
+        </span>
+      </StatusRow>
+    )
+  }
+
   const branchLabel = status.detached ? s.detached : status.branch || s.noBranch
   // The kebab offers branching off the trunk and/or the current branch. The
   // worktree-add bases the new branch on `base` (a branch name; undefined =
@@ -204,7 +220,7 @@ export const CodingStatusRow = memo(function CodingStatusRow({
         <StatusRow
           // The base "where am I working" strip is part of the composer surface
           // itself, so it inherits the composer's width and clipped top radius.
-          className="coding-status-bar min-h-7 rounded-t-[inherit] rounded-b-none border-b border-(--ui-stroke-tertiary) px-3.5 py-1.5 hover:bg-transparent"
+          className={CODING_ROW_CLASS}
           // Static branch glyph — never the loading spinner. This row only renders
           // once `status` exists, so a spinner here only ever fired on *refreshes*
           // of an already-loaded repo (window focus, turn settle), reading as an
diff --git a/apps/desktop/src/app/gateway/hooks/use-gateway-boot.ts b/apps/desktop/src/app/gateway/hooks/use-gateway-boot.ts
index f02ee69f5..4d4cfd6eb 100644
--- a/apps/desktop/src/app/gateway/hooks/use-gateway-boot.ts
+++ b/apps/desktop/src/app/gateway/hooks/use-gateway-boot.ts
@@ -4,7 +4,6 @@ import { useEffect, useRef } from 'react'
 import type { HermesConnection } from '@/global'
 import { HermesGateway } from '@/hermes'
 import { translateNow } from '@/i18n'
-import { desktopDefaultCwd } from '@/lib/desktop-fs'
 import {
   $desktopBoot,
   applyDesktopBootProgress,
@@ -27,14 +26,10 @@ import { $gatewaySwitching, wipeSessionListsForGatewaySwitch } from '@/store/gat
 import { notify, notifyError } from '@/store/notifications'
 import { $activeGatewayProfile, normalizeProfileKey, touchActiveGatewayBackend } from '@/store/profile'
 import {
-  $activeSessionId,
   $connection,
-  $currentCwd,
   $sessions,
   ensureDefaultWorkspaceCwd,
   setConnection,
-  setCurrentBranch,
-  setCurrentCwd,
   setSessionsLoading
 } from '@/store/session'
 import { $attentionSessionIds, $workingSessionIds, resetTileRuntimeBindings } from '@/store/session-states'
@@ -247,16 +242,11 @@ export function useGatewayBoot({
       }
     }
 
-    // Seed the working dir from the backend default on a fresh view (nothing
-    // open yet). Shared by boot + soft switch.
+    // Seed only an explicitly configured desktop project. The backend default
+    // is an execution directory, not a project selection; adopting it here can
+    // expose a parent Git checkout on startup, reconnect, or renderer reload.
     async function seedDefaultCwd() {
       await ensureDefaultWorkspaceCwd()
-      const remoteDefault = await desktopDefaultCwd().catch(() => null)
-
-      if (remoteDefault?.cwd && !$activeSessionId.get() && !$currentCwd.get()) {
-        setCurrentCwd(remoteDefault.cwd)
-        setCurrentBranch(remoteDefault.branch || '')
-      }
     }
 
     // Soft gateway-mode apply: main tore down the primary without reloading.
diff --git a/apps/desktop/src/app/session/hooks/use-session-actions.test.tsx b/apps/desktop/src/app/session/hooks/use-session-actions.test.tsx
index 35110e481..7fc1aa386 100644
--- a/apps/desktop/src/app/session/hooks/use-session-actions.test.tsx
+++ b/apps/desktop/src/app/session/hooks/use-session-actions.test.tsx
@@ -546,7 +546,7 @@ describe('createBackendSessionForSend profile routing', () => {
     })
   })
 
-  it('falls back to the entered project cwd when the current cwd is blank', async () => {
+  it('does not inherit the sidebar project scope when the current cwd is blank', async () => {
     const params = await createWith(() => {
       $projectTree.set([
         {
@@ -561,7 +561,7 @@ describe('createBackendSessionForSend profile routing', () => {
       $currentCwd.set('')
     })
 
-    expect(params).toMatchObject({ cwd: '/repo/app' })
+    expect(params).not.toHaveProperty('cwd')
   })
 })
 
diff --git a/apps/desktop/src/store/projects.test.ts b/apps/desktop/src/store/projects.test.ts
index 190c3d834..6fbe72af4 100644
--- a/apps/desktop/src/store/projects.test.ts
+++ b/apps/desktop/src/store/projects.test.ts
@@ -131,7 +131,22 @@ describe('resolveNewSessionCwd', () => {
     expect(resolveNewSessionCwd()).toBe('')
   })
 
-  it('still falls back to the configured default outside Home', () => {
+  it('falls back to the configured default outside Home', () => {
+    expect(resolveNewSessionCwd()).toBe('/home/user/configured')
+  })
+
+  it('does not inherit a persisted project scope for a new chat', () => {
+    $projectTree.set([
+      {
+        id: 'p_previous',
+        label: 'Previous project',
+        path: '/repos/previous',
+        repos: [],
+        sessionCount: 0
+      }
+    ])
+    enterProject('p_previous')
+
     expect(resolveNewSessionCwd()).toBe('/home/user/configured')
   })
 })
diff --git a/apps/desktop/src/store/projects.ts b/apps/desktop/src/store/projects.ts
index 8b0f9cbc9..f958cfe29 100644
--- a/apps/desktop/src/store/projects.ts
+++ b/apps/desktop/src/store/projects.ts
@@ -161,12 +161,11 @@ export function exitProjectScope(): void {
   $projectScope.set(ALL_PROJECTS)
 }
 
-// The cwd a NEW chat should start in. The "active project" is just an atom
-// ($projectScope) — so when you're inside a project, a new session (cmd-n, the
-// trunk "+") starts at that project's root (its primary repo = the default-branch
-// checkout) instead of inheriting whatever unrelated worktree the live cwd
-// drifted into. Outside a project it falls back to the plain default (detached),
-// so a bare new chat shows no branch.
+// The cwd a bare NEW chat should start in. Project/worktree "+" actions pass an
+// explicit workspace target through startSessionInWorkspace; the sidebar's
+// persisted view scope is not session authority. Letting it choose the cwd made
+// renderer reloads and reconnects attach the last-viewed (sometimes install)
+// repository to an unrelated new chat.
 export function resolveNewSessionCwd(): string {
   const scope = $projectScope.get()
 
@@ -176,15 +175,6 @@ export function resolveNewSessionCwd(): string {
     return ''
   }
 
-  if (scope !== ALL_PROJECTS) {
-    const project = $projectTree.get().find(node => node.id === scope)
-    const cwd = (project?.path || project?.repos.find(repo => repo.path)?.path || '').trim()
-
-    if (cwd) {
-      return cwd
-    }
-  }
-
   return workspaceCwdForNewSession()
 }
 
diff --git a/apps/desktop/src/store/session.test.ts b/apps/desktop/src/store/session.test.ts
index b44c17b2b..9a60a5827 100644
--- a/apps/desktop/src/store/session.test.ts
+++ b/apps/desktop/src/store/session.test.ts
@@ -12,6 +12,7 @@ import {
   $sessions,
   $unreadFinishedSessionIds,
   applyConfiguredDefaultProjectDir,
+  ensureDefaultWorkspaceCwd,
   getRememberedRoute,
   getRememberedSessionId,
   mergeSessionPage,
@@ -281,6 +282,7 @@ describe('workspaceCwdForNewSession', () => {
     window.localStorage.removeItem('hermes.desktop.workspace-cwd')
     window.localStorage.removeItem('hermes.desktop.workspace-cwd.remote.http%3A%2F%2Fbackend-a.default')
     window.localStorage.removeItem('hermes.desktop.workspace-cwd.remote.http%3A%2F%2Fbackend-b.default')
+    delete (window as unknown as { hermesDesktop?: unknown }).hermesDesktop
   })
 
   it('prefers the configured default over the sticky remembered workspace', () => {
@@ -309,6 +311,27 @@ describe('workspaceCwdForNewSession', () => {
     expect(workspaceCwdForNewSession()).toBe('')
   })
 
+  it('clears a stale fresh-draft cwd on reconnect without erasing chat history', async () => {
+    window.localStorage.setItem('hermes.desktop.workspace-cwd', '/installation/repository')
+    $currentCwd.set('/installation/repository')
+    $connection.set({ baseUrl: 'http://backend-a', mode: 'remote' } as never)
+    ;(window as unknown as { hermesDesktop: unknown }).hermesDesktop = {
+      sanitizeWorkspaceCwd: vi.fn(),
+      settings: {
+        getDefaultProjectDir: vi.fn(async () => ({
+          defaultLabel: '/home/user',
+          dir: null,
+          resolvedCwd: '/installation/repository/data/user'
+        }))
+      }
+    }
+
+    await ensureDefaultWorkspaceCwd()
+
+    expect($currentCwd.get()).toBe('')
+    expect(window.localStorage.getItem('hermes.desktop.workspace-cwd')).toBe('/installation/repository')
+  })
+
   it('does not rewrite the live cwd while a session is active', () => {
     $activeSessionId.set('sess-1')
     $currentCwd.set('/live/session/path')
@@ -318,7 +341,7 @@ describe('workspaceCwdForNewSession', () => {
     expect(workspaceCwdForNewSession()).toBe('/home/user/configured')
   })
 
-  it('keeps remote workspace memory separate from local and other remotes', () => {
+  it('keeps remote workspace memory separate without attaching it to new chats', () => {
     window.localStorage.setItem('hermes.desktop.workspace-cwd', '/local/project')
     $currentCwd.set('/live/session/path')
     $connection.set({ baseUrl: 'http://backend-a', mode: 'remote' } as never)
@@ -326,13 +349,13 @@ describe('workspaceCwdForNewSession', () => {
     expect(workspaceCwdForNewSession()).toBe('')
 
     setCurrentCwd('/backend/project-a')
-    expect(workspaceCwdForNewSession()).toBe('/backend/project-a')
+    expect(workspaceCwdForNewSession()).toBe('')
 
     $connection.set({ baseUrl: 'http://backend-b', mode: 'remote' } as never)
     expect(workspaceCwdForNewSession()).toBe('')
 
     setCurrentCwd('/backend/project-b')
-    expect(workspaceCwdForNewSession()).toBe('/backend/project-b')
+    expect(workspaceCwdForNewSession()).toBe('')
 
     // Back on local with no configured default: a bare new chat is detached and
     // never reads the remote keys (nor inherits the sticky local workspace).
diff --git a/apps/desktop/src/store/session.ts b/apps/desktop/src/store/session.ts
index c9d7c1904..9e194fba8 100644
--- a/apps/desktop/src/store/session.ts
+++ b/apps/desktop/src/store/session.ts
@@ -125,9 +125,14 @@ export async function syncConfiguredDefaultProjectDir(): Promise<string> {
   return configuredDefaultProjectDir
 }
 
-/** Align the renderer workspace with the main-process default (home dir when
- *  packaged, optional Settings override). Clears stale install-dir paths that
- *  PR #37586's localStorage stickiness can preserve across the #37536 fix. */
+/** Align a fresh renderer draft with an explicitly configured project.
+ *
+ * The process/backend default is only an execution directory. Treating it (or
+ * the last workspace stored in localStorage) as the draft's project makes a
+ * packaged app launched beneath a Git checkout expose that checkout in Review.
+ * A chat attaches only through the explicit default setting or a per-chat
+ * workspace action.
+ */
 export async function ensureDefaultWorkspaceCwd(): Promise<void> {
   const sanitize = window.hermesDesktop?.sanitizeWorkspaceCwd
 
@@ -138,31 +143,28 @@ export async function ensureDefaultWorkspaceCwd(): Promise<void> {
   await syncConfiguredDefaultProjectDir()
   const configured = getConfiguredDefaultProjectDir()
 
-  const seedLiveCwd = (cwd: string) => {
-    if (cwd && !$activeSessionId.get()) {
-      setCurrentCwd(cwd)
-    }
+  if ($activeSessionId.get()) {
+    return
   }
 
-  const remembered = getRememberedWorkspaceCwd()
-
+  // A local path cannot be an explicit default for a remote backend. Remote
+  // projects are attached through that connection's project/session actions.
   if ($connection.get()?.mode === 'remote') {
-    seedLiveCwd(remembered)
+    setCurrentCwdTransient('')
 
     return
   }
 
   if (configured) {
     const { cwd } = await sanitize(configured)
-    seedLiveCwd(cwd)
+    setCurrentCwd(cwd)
 
     return
   }
 
-  if (remembered) {
-    const { cwd } = await sanitize(remembered)
-    seedLiveCwd(cwd)
-  }
+  // Do not erase the remembered value: an attached chat may still use it as
+  // presentation history. It simply is not authority for a brand-new draft.
+  setCurrentCwdTransient('')
 }
 
 export function applyConfiguredDefaultProjectDir(dir: null | string | undefined): void {
@@ -543,7 +545,7 @@ export const setNewChatWorkspaceTarget = (next: NewChatWorkspaceTarget): number
 
 export const workspaceCwdForNewSession = (): string => {
   if ($connection.get()?.mode === 'remote') {
-    return getRememberedWorkspaceCwd()
+    return ''
   }
 
   // A bare new chat starts DETACHED — no inherited cwd, so the composer's coding
diff --git a/apps/desktop/src/store/updates.test.ts b/apps/desktop/src/store/updates.test.ts
index 9dd1cbed6..d2b4f4311 100644
--- a/apps/desktop/src/store/updates.test.ts
+++ b/apps/desktop/src/store/updates.test.ts
@@ -134,7 +134,7 @@ describe('reportBackendContract', () => {
   })
 
   it('dismisses the toast when the backend meets the contract', () => {
-    reportBackendContract(5)
+    reportBackendContract(6)
     expect(dismissSpy).toHaveBeenCalledWith('backend-contract-skew')
     expect(notifySpy).not.toHaveBeenCalled()
   })
@@ -174,8 +174,8 @@ describe('reportBackendContract', () => {
     lastToast().onDismiss()
     notifySpy.mockClear()
 
-    reportBackendContract(5) // backend updated → satisfied, snooze cleared
-    reportBackendContract(4) // a later regression must warn immediately
+    reportBackendContract(6) // backend updated → satisfied, snooze cleared
+    reportBackendContract(5) // a later regression must warn immediately
     expect(notifySpy).toHaveBeenCalledTimes(1)
   })
 })
diff --git a/apps/desktop/src/store/updates.ts b/apps/desktop/src/store/updates.ts
index c5e7b8587..2464e6a82 100644
--- a/apps/desktop/src/store/updates.ts
+++ b/apps/desktop/src/store/updates.ts
@@ -94,7 +94,8 @@ function isUpdateToastSnoozed(): boolean {
 // v3: requires approvals.mode config RPCs and session.info reconciliation.
 // v4: requires explicit Fast-off session creation and session-scoped Fast edits.
 // v5: requires raised WebSocket frame size for large one-shot file.attach.
-const REQUIRED_BACKEND_CONTRACT = 5
+// v6: requires implicit desktop execution cwd to remain projectless in session.info.
+const REQUIRED_BACKEND_CONTRACT = 6
 const SKEW_TOAST_ID = 'backend-contract-skew'
 // The contract check runs on every session.resume (applyRuntimeInfo), so
 // without a snooze the warning re-popped on every thread the user opened, even
diff --git a/tests/test_tui_gateway_server.py b/tests/test_tui_gateway_server.py
index 7456f116b..53f8215a0 100644
--- a/tests/test_tui_gateway_server.py
+++ b/tests/test_tui_gateway_server.py
@@ -4876,6 +4876,110 @@ def test_ensure_session_db_row_defaults_desktop_to_no_workspace(monkeypatch, tmp
     ]
 
 
+def test_session_create_keeps_implicit_desktop_cwd_out_of_live_project_info(
+    monkeypatch, tmp_path
+):
+    """The gateway may execute in a neutral directory without attaching it.
+
+    A packaged desktop launched from a directory nested beneath a Git checkout
+    used to report that internal cwd (and its parent branch) as session.info,
+    exposing the installation repository in the composer and Review.
+    """
+    install_repo = tmp_path / "Hermes-Local"
+    neutral_cwd = install_repo / "data" / "user"
+    neutral_cwd.mkdir(parents=True)
+
+    monkeypatch.setattr(server, "_completion_cwd", lambda params=None: str(neutral_cwd))
+    monkeypatch.setattr(server, "_schedule_agent_build", lambda *a, **k: None)
+    monkeypatch.setattr(server, "_schedule_session_cap_enforcement", lambda: None)
+    monkeypatch.setattr(server, "_git_branch_for_cwd", lambda cwd: "main" if cwd else "")
+    monkeypatch.setattr(
+        server,
+        "_project_info_for_cwd",
+        lambda cwd: {"id": "installation"} if cwd else None,
+    )
+
+    resp = server.handle_request(
+        {
+            "id": "projectless-create",
+            "method": "session.create",
+            "params": {"cols": 80, "source": "desktop"},
+        }
+    )
+    sid = resp["result"]["session_id"]
+
+    try:
+        assert server._sessions[sid]["cwd"] == str(neutral_cwd)
+        assert server._sessions[sid]["explicit_cwd"] is False
+        assert resp["result"]["info"]["cwd"] == ""
+        assert resp["result"]["info"]["branch"] == ""
+        assert resp["result"]["info"]["project"] is None
+    finally:
+        server._sessions.pop(sid, None)
+
+
+def test_session_create_reports_an_explicit_desktop_workspace(monkeypatch, tmp_path):
+    workspace = tmp_path / "selected-project"
+    workspace.mkdir()
+
+    monkeypatch.setattr(server, "_completion_cwd", lambda params=None: str(workspace))
+    monkeypatch.setattr(server, "_schedule_agent_build", lambda *a, **k: None)
+    monkeypatch.setattr(server, "_schedule_session_cap_enforcement", lambda: None)
+    monkeypatch.setattr(server, "_git_branch_for_cwd", lambda cwd: "feature" if cwd else "")
+    monkeypatch.setattr(
+        server,
+        "_project_info_for_cwd",
+        lambda cwd: {"id": "selected"} if cwd else None,
+    )
+
+    resp = server.handle_request(
+        {
+            "id": "attached-create",
+            "method": "session.create",
+            "params": {
+                "cols": 80,
+                "cwd": str(workspace),
+                "source": "desktop",
+            },
+        }
+    )
+    sid = resp["result"]["session_id"]
+
+    try:
+        assert server._sessions[sid]["explicit_cwd"] is True
+        assert resp["result"]["info"]["cwd"] == str(workspace)
+        assert resp["result"]["info"]["branch"] == "feature"
+        assert resp["result"]["info"]["project"] == {"id": "selected"}
+    finally:
+        server._sessions.pop(sid, None)
+
+
+def test_live_reconnect_keeps_implicit_desktop_cwd_projectless(monkeypatch, tmp_path):
+    """The already-live resume path uses fallback info while the agent builds."""
+    neutral_cwd = tmp_path / "Hermes-Local" / "data" / "user"
+    neutral_cwd.mkdir(parents=True)
+    session = {
+        "agent": None,
+        "cwd": str(neutral_cwd),
+        "explicit_cwd": False,
+        "source": "desktop",
+    }
+
+    monkeypatch.setattr(server, "_git_branch_for_cwd", lambda cwd: "main" if cwd else "")
+    monkeypatch.setattr(
+        server,
+        "_project_info_for_cwd",
+        lambda cwd: {"id": "installation"} if cwd else None,
+    )
+
+    info = server._fallback_session_info(session)
+
+    assert info["cwd"] == ""
+    assert info["branch"] == ""
+    assert info["project"] is None
+    assert info["desktop_contract"] == server.DESKTOP_BACKEND_CONTRACT
+
+
 def test_ensure_session_db_row_persists_session_model_override(monkeypatch):
     """The session's composer pick (model + effort + fast) must own the DB row.
 
@@ -10786,6 +10890,7 @@ def test_session_branch_writes_to_parent_profile_db(monkeypatch, tmp_path):
             seen["created"] = new_key
             seen["parent"] = kwargs.get("parent_session_id")
             seen["profile_name"] = kwargs.get("profile_name")
+            seen["cwd"] = kwargs.get("cwd")
 
         def append_message(self, **kwargs):
             seen["msgs"].append(kwargs)
@@ -10795,7 +10900,7 @@ def test_session_branch_writes_to_parent_profile_db(monkeypatch, tmp_path):
             return True
 
         def get_session(self, key):
-            return {"id": key, "cwd": str(tmp_path)}
+            return {"id": key, "cwd": seen.get("cwd")}
 
         def update_session_cwd(self, *a, **k):
             return None
@@ -10815,7 +10920,8 @@ def test_session_branch_writes_to_parent_profile_db(monkeypatch, tmp_path):
         "running": False,
         "cols": 80,
         "profile_home": str(profile_home),
-        "source": "tui",
+        "source": "desktop",
+        "explicit_cwd": False,
         "agent": FakeAgent(),
         "created_at": 1.0,
         "last_active": 1.0,
@@ -10851,12 +10957,15 @@ def test_session_branch_writes_to_parent_profile_db(monkeypatch, tmp_path):
         # The branch row is self-describing: stamped with the parent's owning
         # profile, not left NULL for aggregators to mis-tag as "default".
         assert seen.get("profile_name") == "mlperf"
+        assert seen.get("cwd") is None
         assert seen.get("title") == (seen["created"], "forked")
         assert len(seen["msgs"]) == 1
         assert seen.get("launch") is None
         assert seen.get("launch_create") is None
         child_sid = resp["result"]["session_id"]
         assert server._sessions[child_sid]["profile_home"] == str(profile_home)
+        assert server._sessions[child_sid]["explicit_cwd"] is False
+        assert resp["result"]["info"]["cwd"] == ""
         # The branched AGENT must be bound to the parent profile's state.db —
         # not just the row. Otherwise its own flushes (and a later compression
         # rotation) land on the launch db, splitting the lineage again.
diff --git a/tests/tui_gateway/test_session_cwd_follow.py b/tests/tui_gateway/test_session_cwd_follow.py
index e5fa5a7c2..aeb313037 100644
--- a/tests/tui_gateway/test_session_cwd_follow.py
+++ b/tests/tui_gateway/test_session_cwd_follow.py
@@ -48,7 +48,12 @@ def session(repo_with_worktree):
     repo, _ = repo_with_worktree
     key = "sess-follow"
     terminal_tool.clear_session_cwd(key)
-    yield {"session_key": key, "cwd": str(repo), "source": "desktop"}
+    yield {
+        "session_key": key,
+        "cwd": str(repo),
+        "explicit_cwd": True,
+        "source": "desktop",
+    }
     terminal_tool.clear_session_cwd(key)
 
 
diff --git a/tui_gateway/methods_session.py b/tui_gateway/methods_session.py
index ea0e1720f..1c0fd3fc2 100644
--- a/tui_gateway/methods_session.py
+++ b/tui_gateway/methods_session.py
@@ -124,6 +124,8 @@ def _(rid, params: dict) -> dict:
     _schedule_agent_build(sid)
     _schedule_session_cap_enforcement()  # trim detached idle sessions over the cap
 
+    display_cwd = _display_session_cwd(_sessions[sid])
+
     return _ok(
         rid,
         {
@@ -148,9 +150,9 @@ def _(rid, params: dict) -> dict:
                 ),
                 "tools": {},
                 "skills": {},
-                "cwd": _sessions[sid]["cwd"],
-                "branch": _git_branch_for_cwd(_sessions[sid]["cwd"]),
-                "project": _project_info_for_cwd(_sessions[sid]["cwd"]),
+                "cwd": display_cwd,
+                "branch": _git_branch_for_cwd(display_cwd),
+                "project": _project_info_for_cwd(display_cwd),
                 "lazy": True,
                 "desktop_contract": DESKTOP_BACKEND_CONTRACT,
                 "profile_name": _response_profile_name(profile),
@@ -368,7 +370,8 @@ def _(rid, params: dict) -> dict:
             target = tip
             found = db.get_session(target) or found
 
-    profile_resume_cwd = str(found.get("cwd") or "").strip() or _profile_configured_cwd(
+    stored_cwd = str(found.get("cwd") or "").strip()
+    profile_resume_cwd = stored_cwd or _profile_configured_cwd(
         profile_home
     )
 
@@ -430,6 +433,7 @@ def _(rid, params: dict) -> dict:
             close_on_disconnect=is_truthy_value(params.get("close_on_disconnect", False)),
             profile_home=profile_home,
             lazy=True,
+            explicit_cwd=bool(stored_cwd),
         )
         if (live := _claim_or_reuse_live(sid, target, record, lease)) is not None:
             return _ok(rid, _reuse_live_payload(*live))
@@ -457,7 +461,9 @@ def _(rid, params: dict) -> dict:
                 "resumed": target,
                 "message_count": len(messages),
                 "messages": messages,
-                "info": _lazy_resume_info(cwd, profile=profile),
+                "info": _lazy_resume_info(
+                    _display_session_cwd(record), profile=profile
+                ),
                 "inflight": None,
                 "running": child_running,
                 "session_key": target,
@@ -519,6 +525,7 @@ def _(rid, params: dict) -> dict:
             close_on_disconnect=is_truthy_value(params.get("close_on_disconnect", False)),
             display_history_prefix=prefix,
             profile_home=profile_home,
+            explicit_cwd=bool(stored_cwd),
             model_override=overrides.get("model_override"),
             resume_runtime_overrides=overrides or None,
         )
@@ -536,7 +543,7 @@ def _(rid, params: dict) -> dict:
             "message_count": len(messages),
             "messages": messages,
             "info": _lazy_resume_info(
-                cwd,
+                _display_session_cwd(record),
                 model=model_override.get("model") or "",
                 provider=overrides.get("provider_override") or "",
                 profile=profile,
@@ -655,6 +662,7 @@ def _(rid, params: dict) -> dict:
                     cwd=profile_resume_cwd,
                     session_db=db,
                     source=source,
+                    explicit_cwd=bool(stored_cwd),
                 )
             finally:
                 if init_home_token is not None:
@@ -2561,7 +2569,7 @@ def _(rid, params: dict) -> dict:
                 # thing that surfaces TUI branches. See issue #20856.
                 model_config={"_branched_from": old_key},
                 parent_session_id=old_key,
-                cwd=_session_cwd(session),
+                cwd=_persisted_session_cwd(session),
                 # The branch stays on its parent's profile. Explicit stamp (not
                 # just the parent-backfill) so it holds even when the parent row
                 # predates the profile_name column.
@@ -2624,6 +2632,7 @@ def _(rid, params: dict) -> dict:
                 session_db=branch_db,
                 source=source,
                 profile_home=parent_home,
+                explicit_cwd=bool(session.get("explicit_cwd")),
             )
         finally:
             if home_token is not None:
diff --git a/tui_gateway/server.py b/tui_gateway/server.py
index fdef04968..f8d3d627c 100644
--- a/tui_gateway/server.py
+++ b/tui_gateway/server.py
@@ -2250,10 +2250,21 @@ def _is_local_terminal_backend() -> bool:
 def _display_session_cwd(session: dict | None) -> str:
     """Session cwd for display/probe surfaces, healed past deleted worktrees.
 
+    A desktop session with no explicit workspace still has an internal cwd so
+    tools can run. That execution detail is not a project attachment and must
+    never reach branch, Review, project-tree, or persisted UI state.
+
     Persists the healed value back to the session row (best-effort, local only)
     so the next load is already coherent and the sidebar lane stops showing a
     session pinned to a vanished path.
     """
+    if (
+        session
+        and _session_source(session) in _LAUNCH_CWD_NOT_A_WORKSPACE
+        and not session.get("explicit_cwd")
+    ):
+        return ""
+
     cwd = _session_cwd(session)
     if not _is_local_terminal_backend():
         return cwd
@@ -4592,7 +4603,8 @@ def _current_profile_name() -> str:
 # v3: adds approvals.mode config RPCs and session.info reconciliation.
 # v4: session.create fast=false is an explicit per-session normal-tier override.
 # v5: uvicorn ws_max_size raised for one-shot base64 file.attach frames (>16 MiB).
-DESKTOP_BACKEND_CONTRACT = 5
+# v6: implicit desktop execution cwd is never reported as an attached workspace.
+DESKTOP_BACKEND_CONTRACT = 6
 
 
 def _session_usage_snapshot(session: dict | None) -> dict:
@@ -6093,6 +6105,7 @@ def _init_session(
     session_db=None,
     source: str | None = None,
     profile_home: str | None = None,
+    explicit_cwd: bool | None = None,
 ):
     now = time.time()
     with _sessions_lock:
@@ -6109,6 +6122,7 @@ def _init_session(
             "attached_images": [],
             "image_counter": 0,
             "cwd": cwd or _completion_cwd(),
+            "explicit_cwd": bool(cwd) if explicit_cwd is None else explicit_cwd,
             "cols": cols,
             "slash_worker": None,
             "show_reasoning": _load_show_reasoning(),
@@ -6148,13 +6162,15 @@ def _init_session(
                 with _sessions_lock:
                     if sid in _sessions:
                         _sessions[sid]["cwd"] = row["cwd"]
+                        _sessions[sid]["explicit_cwd"] = True
             else:
                 try:
-                    _cwd = _sessions[sid]["cwd"]
-                    if hasattr(db, "update_session_cwd"):
-                        db.update_session_cwd(key, _cwd)
-                    # git branch/root probes run off the hot path (see _set_session_cwd).
-                    _persist_session_git_meta(_sessions[sid], _cwd)
+                    persisted_cwd = _persisted_session_cwd(_sessions[sid])
+                    if persisted_cwd:
+                        if hasattr(db, "update_session_cwd"):
+                            db.update_session_cwd(key, persisted_cwd)
+                        # git branch/root probes run off the hot path (see _set_session_cwd).
+                        _persist_session_git_meta(_sessions[sid], persisted_cwd)
                 except Exception:
                     logger.debug(
                         "failed to persist resumed session cwd", exc_info=True
@@ -7204,6 +7220,7 @@ def _deferred_session_record(
     display_history_prefix: list | None = None,
     profile_home: Path | None = None,
     lazy: bool = False,
+    explicit_cwd: bool = False,
     model_override=None,
     resume_runtime_overrides: dict | None = None,
 ) -> dict:
@@ -7222,7 +7239,7 @@ def _deferred_session_record(
         "cwd": cwd,
         "display_history_prefix": display_history_prefix or [],
         "edit_snapshots": {},
-        "explicit_cwd": False,
+        "explicit_cwd": explicit_cwd,
         "history": history,
         "history_lock": threading.Lock(),
         "history_version": 0,
@@ -7371,9 +7388,11 @@ def _fallback_session_info(session: dict) -> dict:
     agent = session.get("agent")
     if agent is not None:
         return _session_info(agent)
-    cwd = _default_session_cwd()
+    cwd = _display_session_cwd(session)
     return {
+        "branch": _git_branch_for_cwd(cwd),
         "cwd": cwd,
+        "desktop_contract": DESKTOP_BACKEND_CONTRACT,
         "project": _project_info_for_cwd(cwd),
         "lazy": True,
         "model": _resolve_model(),
-- 
2.55.0.windows.3

