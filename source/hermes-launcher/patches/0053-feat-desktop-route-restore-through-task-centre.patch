From 7acd4f20c6cccad15000ca9691ca4eeb78654760 Mon Sep 17 00:00:00 2001
From: OpenAI <openai@localhost>
Date: Wed, 5 Aug 2026 04:38:12 +0000
Subject: [PATCH 13/14] feat(desktop): route restore through Task Centre

---
 apps/desktop/src/app/chat/sidebar/index.tsx       |  7 +++++++
 .../src/app/local-workstation/task-centre.tsx     |  5 ++++-
 apps/desktop/src/app/local-workstation/types.ts   | 15 ++++++++++++++-
 apps/desktop/src/app/routes.ts                    |  3 +++
 4 files changed, 28 insertions(+), 2 deletions(-)

diff --git a/apps/desktop/src/app/chat/sidebar/index.tsx b/apps/desktop/src/app/chat/sidebar/index.tsx
index d1c5f70..d8c42ef 100644
--- a/apps/desktop/src/app/chat/sidebar/index.tsx
+++ b/apps/desktop/src/app/chat/sidebar/index.tsx
@@ -110,6 +110,7 @@ import {
   MESSAGING_ROUTE,
   MODELS_ROUTE,
   PROJECTS_ROUTE,
+  RESTORE_ROUTE,
   SECURITY_ROUTE,
   SERVICES_ROUTE,
   SESSIONS_ROUTE,
@@ -247,6 +248,12 @@ const SIDEBAR_NAV: SidebarNavItem[] = [
     icon: props => <Codicon name="shield" {...props} />,
     route: SECURITY_ROUTE
   },
+  {
+    id: 'restore',
+    label: '',
+    icon: props => <Codicon name="history" {...props} />,
+    route: RESTORE_ROUTE
+  },
   {
     id: 'logs',
     label: '',
diff --git a/apps/desktop/src/app/local-workstation/task-centre.tsx b/apps/desktop/src/app/local-workstation/task-centre.tsx
index 67499e2..ccb96f7 100644
--- a/apps/desktop/src/app/local-workstation/task-centre.tsx
+++ b/apps/desktop/src/app/local-workstation/task-centre.tsx
@@ -40,6 +40,7 @@ const ACTION_COPY: Record<LocalActionTask['action'], { feature: string; label: s
   diagnostics: { feature: 'Services', label: 'Diagnostics', route: '/services' },
   repair: { feature: 'Services', label: 'Repair', route: '/services' },
   restart: { feature: 'Services', label: 'Restart', route: '/services' },
+  restore: { feature: 'Restore', label: 'Restore', route: '/restore' },
   security: { feature: 'Security', label: 'Security scan', route: '/security' },
   start: { feature: 'Services', label: 'Start', route: '/services' },
   stop: { feature: 'Services', label: 'Stop', route: '/services' },
@@ -114,7 +115,9 @@ function relativeTime(value: string, now = Date.now()): string {
 function taskStage(task: LocalActionTask): string {
   if (task.status === 'queued') {return 'Waiting for resources'}
 
-  if (task.status === 'cancelling') {return 'Stopping owned process'}
+  if (task.status === 'cancelling') {
+    return task.action === 'restore' ? 'Waiting for a safe restore cancellation boundary' : 'Stopping owned process'
+  }
 
   if (task.status === 'running') {return task.stage || `${ACTION_COPY[task.action].label} in progress`}
 
diff --git a/apps/desktop/src/app/local-workstation/types.ts b/apps/desktop/src/app/local-workstation/types.ts
index 1d6ae8a..a35958a 100644
--- a/apps/desktop/src/app/local-workstation/types.ts
+++ b/apps/desktop/src/app/local-workstation/types.ts
@@ -27,6 +27,7 @@ export type LocalAction =
   | 'diagnostics'
   | 'repair'
   | 'restart'
+  | 'restore'
   | 'security'
   | 'start'
   | 'stop'
@@ -99,6 +100,7 @@ export interface LocalActionTask {
     pid: null | number
   }
   progress?: null | {
+    cancellable?: boolean
     completedUnits: null | number
     counters: Record<string, number>
     message: null | string
@@ -109,7 +111,7 @@ export interface LocalActionTask {
   queuedAt: string
   resources: {
     mode: 'exclusive' | 'shared'
-    resource: 'model-runtime' | 'user-data' | 'workstation'
+    resource: 'installation' | 'model-runtime' | 'user-data' | 'workstation'
   }[]
   result: null | {
     kind: 'archive' | 'report' | 'runtime-state'
@@ -185,6 +187,16 @@ export interface LocalModel {
   userManaged: boolean
 }
 
+export interface LocalBackup {
+  id: string
+  modifiedAt: string
+  name: string
+  path: string
+  sha256: null | string
+  sizeBytes: number
+  verified: boolean
+}
+
 export interface LocalWorkstationSettings {
   autoTuning: {
     batchThreads: number
@@ -211,6 +223,7 @@ export interface LocalWorkstationSettings {
 
 export interface LocalWorkstationSnapshot {
   actions: Record<LocalAction, boolean>
+  backups: LocalBackup[]
   lifecycle: {
     identityMatches: boolean
     switchingModel: null | {
diff --git a/apps/desktop/src/app/routes.ts b/apps/desktop/src/app/routes.ts
index 46875a0..ddfa625 100644
--- a/apps/desktop/src/app/routes.ts
+++ b/apps/desktop/src/app/routes.ts
@@ -31,6 +31,7 @@ export const SESSIONS_ROUTE = '/sessions'
 export const PROJECTS_ROUTE = '/projects'
 export const BENCHMARKS_ROUTE = '/benchmarks'
 export const SECURITY_ROUTE = '/security'
+export const RESTORE_ROUTE = '/restore'
 export const LOGS_ROUTE = '/logs'
 export const ABOUT_ROUTE = '/about'
 export const LEGACY_TOOLS_REDIRECT_ROUTE = SKILLS_ROUTE
@@ -79,6 +80,7 @@ export type AppRouteId =
   | 'projects'
   | 'benchmarks'
   | 'security'
+  | 'restore'
   | 'logs'
   | 'about'
 
@@ -113,6 +115,7 @@ export const APP_ROUTES = [
   { id: 'projects', path: PROJECTS_ROUTE, view: 'workstation' },
   { id: 'benchmarks', path: BENCHMARKS_ROUTE, view: 'workstation' },
   { id: 'security', path: SECURITY_ROUTE, view: 'workstation' },
+  { id: 'restore', path: RESTORE_ROUTE, view: 'workstation' },
   { id: 'logs', path: LOGS_ROUTE, view: 'workstation' },
   { id: 'about', path: ABOUT_ROUTE, view: 'workstation' }
 ] as const satisfies readonly AppRoute[]
-- 
2.47.3

