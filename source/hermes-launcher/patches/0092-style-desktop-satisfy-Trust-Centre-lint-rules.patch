From e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2e2 Mon Sep 17 00:00:00 2001
From: OpenAI <openai@localhost>
Date: Sat, 8 Aug 2026 04:28:00 +0100
Subject: [PATCH] style(desktop): satisfy Trust Centre lint rules

---
diff --git a/apps/desktop/electron/hermes-local-control.ts b/apps/desktop/electron/hermes-local-control.ts
index 150244e9e61ad6229b9fc01bc98d95c4e087073f..7e73850b327d44107ec0ac44815e9c2b4cb4e9f0 100644
--- a/apps/desktop/electron/hermes-local-control.ts
+++ b/apps/desktop/electron/hermes-local-control.ts
@@ -7,7 +7,6 @@ import path from 'node:path'
 import { promisify } from 'node:util'
 
 import { app, ipcMain, shell } from 'electron'
-import { registerHermesLocalTrustCentreIpc } from './hermes-local-trust-centre'
 
 import {
   type HermesLocalDashboardBounds,
@@ -51,6 +50,7 @@ import {
   transitionTask
 } from './hermes-local-task-model'
 import { loadTaskStore, saveTaskStore } from './hermes-local-task-store'
+import { registerHermesLocalTrustCentreIpc } from './hermes-local-trust-centre'
 
 const execFileAsync = promisify(execFile)
 const MAX_TASK_OUTPUT = 128 * 1024
diff --git a/apps/desktop/src/app/local-workstation/trust-centre.tsx b/apps/desktop/src/app/local-workstation/trust-centre.tsx
index 1d9afc8847471e710c94824926fa446353d2300f..af41c63f3f04af0c2eb3cfb1ade5999526db95b1 100644
--- a/apps/desktop/src/app/local-workstation/trust-centre.tsx
+++ b/apps/desktop/src/app/local-workstation/trust-centre.tsx
@@ -1,5 +1,5 @@
-import { useCallback, useEffect, useMemo, useState, type ReactNode } from 'react'
 import { IconAlertTriangle, IconDownload, IconRefresh, IconShieldLock } from '@tabler/icons-react'
+import { type ReactNode, useCallback, useEffect, useMemo, useState } from 'react'
 
 import { Button } from '@/components/ui/button'
 import { cn } from '@/lib/utils'
@@ -43,7 +43,9 @@ function Panel({ children, title }: { children: ReactNode; title?: string }) {
 }
 
 function shortHash(value?: string) {
-  if (!value) return 'not recorded'
+  if (!value) {
+    return 'not recorded'
+  }
   return value.length > 20 ? `${value.slice(0, 12)}…${value.slice(-8)}` : value
 }
 
@@ -89,7 +91,9 @@ export function TrustCentre() {
   )
 
   useEffect(() => {
-    if (!snapshot || !selected) return
+    if (!snapshot || !selected) {
+      return
+    }
     const grant = grantFor(snapshot, selected.identity.id)
     setCapabilities(
       snapshot.activeGrants
@@ -113,7 +117,9 @@ export function TrustCentre() {
   }
 
   const applyPolicy = async (override?: Partial<TrustPolicyInput>) => {
-    if (!selected || !managed) return
+    if (!selected || !managed) {
+      return
+    }
     const input: TrustPolicyInput = {
       capabilities,
       confirmation,
diff --git a/apps/desktop/src/global.d.ts b/apps/desktop/src/global.d.ts
index 539b94e80b6d718d6d1ffb4593d8fd0e6d2503d0..c4a4e6613e794a2e8cd2297409a0f4dc0c4f600a 100644
--- a/apps/desktop/src/global.d.ts
+++ b/apps/desktop/src/global.d.ts
@@ -1,5 +1,6 @@
 import type { GatewayWsUrlResult } from '@hermes/shared'
 
+import type { TrustCentreSnapshot, TrustIntegrationRecord, TrustPolicyInput } from './app/local-workstation/trust-types'
 import type {
   HermesLocalDashboardBounds,
   HermesLocalDashboardState,
@@ -13,7 +14,6 @@ import type {
   LocalWorkstationSettings,
   LocalWorkstationSnapshot
 } from './app/local-workstation/types'
-import type { TrustCentreSnapshot, TrustIntegrationRecord, TrustPolicyInput } from './app/local-workstation/trust-types'
 import type { WakeIndicatorState } from './lib/wake-indicator'
 import type {
   PetOverlayBounds,
-- 
2.51.0
