From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: xdCloudy <52116030+xdCloudy@users.noreply.github.com>
Date: Tue, 28 Jul 2026 10:04:06 +0100
Subject: [PATCH 04/30] feat(desktop): add current-user launch at login

---
 .../e2e/hermes-local-workstation.spec.ts      | 37 ++++++++
 .../electron/hermes-local-control.test.ts     | 16 ++++
 apps/desktop/electron/hermes-local-control.ts | 64 +++++++++++++-
 apps/desktop/electron/preload.ts              |  4 +
 .../src/app/local-workstation/index.tsx       | 88 +++++++++++++------
 .../src/app/local-workstation/types.ts        |  7 ++
 apps/desktop/src/global.d.ts                  |  4 +
 7 files changed, 193 insertions(+), 27 deletions(-)

diff --git a/apps/desktop/e2e/hermes-local-workstation.spec.ts b/apps/desktop/e2e/hermes-local-workstation.spec.ts
index d486230a5..4544a3831 100644
--- a/apps/desktop/e2e/hermes-local-workstation.spec.ts
+++ b/apps/desktop/e2e/hermes-local-workstation.spec.ts
@@ -8,6 +8,22 @@ const ROOT = process.env.HERMES_LOCAL_ROOT || 'D:\\Hermes-Local'
 const LAUNCHER = process.env.HERMES_LOCAL_LAUNCHER_PATH || path.join(ROOT, 'dist', 'Hermes Launcher.exe')
 const ENABLED = process.env.HERMES_LOCAL_ACCEPTANCE === '1'
 
+interface LoginItemStatus {
+  available: boolean
+  enabled: boolean
+}
+
+interface LoginItemWindow {
+  hermesDesktop: {
+    localWorkstation: {
+      loginItem: {
+        get: () => Promise<LoginItemStatus>
+        set: (enabled: boolean) => Promise<LoginItemStatus>
+      }
+    }
+  }
+}
+
 async function assertRendererCsp(page: Page) {
   const policy = await page
     .locator('meta[http-equiv="Content-Security-Policy"]')
@@ -112,6 +128,27 @@ test.describe('Hermes Local packaged workstation', () => {
       // unexpected console errors; Chromium logs the expected CSP violation.
       await assertRendererCsp(page)
 
+      const originalLoginItem = await page.evaluate(() =>
+        (window as unknown as LoginItemWindow).hermesDesktop.localWorkstation.loginItem.get(),
+      )
+
+      const toggledLoginItem = await page.evaluate(
+        enabled => (window as unknown as LoginItemWindow).hermesDesktop.localWorkstation.loginItem.set(enabled),
+        !originalLoginItem.enabled,
+      )
+
+      try {
+        expect(toggledLoginItem.available).toBe(true)
+        expect(toggledLoginItem.enabled).toBe(!originalLoginItem.enabled)
+      } finally {
+        const restoredLoginItem = await page.evaluate(
+          enabled => (window as unknown as LoginItemWindow).hermesDesktop.localWorkstation.loginItem.set(enabled),
+          originalLoginItem.enabled,
+        )
+
+        expect(restoredLoginItem.enabled).toBe(originalLoginItem.enabled)
+      }
+
       page.on('console', message => {
         if (message.type() === 'error') {
           rendererErrors.push(`console: ${message.text()}`)
diff --git a/apps/desktop/electron/hermes-local-control.test.ts b/apps/desktop/electron/hermes-local-control.test.ts
index 7b35b0b6a..c4f5e0daf 100644
--- a/apps/desktop/electron/hermes-local-control.test.ts
+++ b/apps/desktop/electron/hermes-local-control.test.ts
@@ -1,6 +1,11 @@
+import { app } from 'electron'
 import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
 
 vi.mock('electron', () => ({
+  app: {
+    getLoginItemSettings: vi.fn(() => ({ openAtLogin: false })),
+    setLoginItemSettings: vi.fn()
+  },
   ipcMain: { handle: vi.fn() },
   shell: {
     openExternal: vi.fn(),
@@ -86,4 +91,15 @@ describe('Hermes Local Electron boundary', () => {
       hermesLocalControlTest.sanitizeEditableProfile({ ...validProfile(), contextTokens: 1024 })
     ).toThrow(/context must be an integer/i)
   })
+
+  it('uses a current-user Windows login item and rejects renderer type confusion', () => {
+    hermesLocalControlTest.setLoginItem(true)
+
+    expect(app.setLoginItemSettings).toHaveBeenCalledWith({
+      args: ['--hermes-local-autostart'],
+      openAtLogin: true,
+      path: process.execPath
+    })
+    expect(() => hermesLocalControlTest.setLoginItem('true')).toThrow(/must be a boolean/i)
+  })
 })
diff --git a/apps/desktop/electron/hermes-local-control.ts b/apps/desktop/electron/hermes-local-control.ts
index 8a206a37a..642f45cdd 100644
--- a/apps/desktop/electron/hermes-local-control.ts
+++ b/apps/desktop/electron/hermes-local-control.ts
@@ -5,13 +5,14 @@ import os from 'node:os'
 import path from 'node:path'
 import { promisify } from 'node:util'
 
-import { ipcMain, shell } from 'electron'
+import { app, ipcMain, shell } from 'electron'
 
 const execFileAsync = promisify(execFile)
 const DEFAULT_ROOT = 'D:\\Hermes-Local'
 const MAX_TASK_OUTPUT = 128 * 1024
 const MAX_LOG_BYTES = 512 * 1024
 const PROFILE_NAME = /^[A-Za-z][A-Za-z0-9 ]{0,31}$/
+const LOGIN_ITEM_ARGUMENTS = ['--hermes-local-autostart']
 
 const ACTION_SCRIPTS = {
   backup: 'Backup-Hermes-Local.ps1',
@@ -303,6 +304,59 @@ function processAlive(pid: unknown): boolean {
   }
 }
 
+function loginItemExecutable(): string {
+  const portableExecutable = process.env.PORTABLE_EXECUTABLE_FILE
+
+  if (portableExecutable && path.isAbsolute(portableExecutable) && fs.existsSync(portableExecutable)) {
+    return path.resolve(portableExecutable)
+  }
+
+  return process.execPath
+}
+
+function loginItemStatus() {
+  if (process.platform !== 'win32') {
+    return {
+      available: false,
+      enabled: false,
+      executable: process.execPath
+    }
+  }
+
+  const executable = loginItemExecutable()
+
+  const settings = app.getLoginItemSettings({
+    args: LOGIN_ITEM_ARGUMENTS,
+    path: executable
+  })
+
+  return {
+    available: true,
+    enabled: Boolean(settings.openAtLogin),
+    executable
+  }
+}
+
+function setLoginItem(enabledValue: unknown) {
+  if (typeof enabledValue !== 'boolean') {
+    throw new Error('Launch-at-login state must be a boolean')
+  }
+
+  if (process.platform !== 'win32') {
+    throw new Error('Launch at login is only available in the Windows workstation')
+  }
+
+  const executable = loginItemExecutable()
+
+  app.setLoginItemSettings({
+    args: LOGIN_ITEM_ARGUMENTS,
+    openAtLogin: enabledValue,
+    path: executable
+  })
+
+  return loginItemStatus()
+}
+
 async function workstationSnapshot() {
   const state = readJson<Record<string, any>>('data\\runtime\\status.json')
   const profiles = readJson<Record<string, any>>('config\\profiles\\profiles.json')
@@ -349,6 +403,7 @@ async function workstationSnapshot() {
         ? fs.statSync(resolveUnderRoot('data\\hermes\\state.db')).size
         : 0
     },
+    startup: loginItemStatus(),
     taskLedger: fs.existsSync(resolveUnderRoot('TASKS.md'))
       ? fs.readFileSync(resolveUnderRoot('TASKS.md'), 'utf8').slice(0, 128 * 1024)
       : '',
@@ -579,6 +634,8 @@ export function registerHermesLocalControlIpc(): void {
   ipcMain.handle('hermes:local:action:start', (_event, action, input) => startActionTask(action, input))
   ipcMain.handle('hermes:local:action:status', (_event, taskId) => getActionTask(taskId))
   ipcMain.handle('hermes:local:logs', (_event, name, lines) => readLog(name, lines))
+  ipcMain.handle('hermes:local:login-item:get', loginItemStatus)
+  ipcMain.handle('hermes:local:login-item:set', (_event, enabled) => setLoginItem(enabled))
   ipcMain.handle('hermes:local:profile:save', (_event, profile) => saveProfile(profile))
   ipcMain.handle('hermes:local:profile:select', (_event, name) => selectProfile(name))
   ipcMain.handle('hermes:local:dashboard:open', async () => {
@@ -598,8 +655,11 @@ export function registerHermesLocalControlIpc(): void {
 export const hermesLocalControlTest = {
   configureHermesLocalDesktopEnvironment,
   localRoot,
+  loginItemExecutable,
+  loginItemStatus,
   powershellExecutable,
   redacted,
   resolveUnderRoot,
-  sanitizeEditableProfile
+  sanitizeEditableProfile,
+  setLoginItem
 }
diff --git a/apps/desktop/electron/preload.ts b/apps/desktop/electron/preload.ts
index 67f05297c..676ea71b4 100644
--- a/apps/desktop/electron/preload.ts
+++ b/apps/desktop/electron/preload.ts
@@ -106,6 +106,10 @@ contextBridge.exposeInMainWorld('hermesDesktop', {
   },
   localWorkstation: {
     snapshot: () => ipcRenderer.invoke('hermes:local:snapshot'),
+    loginItem: {
+      get: () => ipcRenderer.invoke('hermes:local:login-item:get'),
+      set: enabled => ipcRenderer.invoke('hermes:local:login-item:set', enabled)
+    },
     startAction: (action, input) => ipcRenderer.invoke('hermes:local:action:start', action, input),
     actionStatus: taskId => ipcRenderer.invoke('hermes:local:action:status', taskId),
     logs: (name, lines) => ipcRenderer.invoke('hermes:local:logs', name, lines),
diff --git a/apps/desktop/src/app/local-workstation/index.tsx b/apps/desktop/src/app/local-workstation/index.tsx
index 92d8df748..54028e50d 100644
--- a/apps/desktop/src/app/local-workstation/index.tsx
+++ b/apps/desktop/src/app/local-workstation/index.tsx
@@ -598,6 +598,7 @@ function SectionContent({
   onNavigate,
   onRun,
   onSaveProfile,
+  onSetLaunchAtLogin,
   onSelectProfile,
   section,
   selectedProfile,
@@ -607,6 +608,7 @@ function SectionContent({
   onNavigate: (path: string) => void
   onRun: (action: LocalAction) => void
   onSaveProfile: (profile: LocalInferenceProfile) => Promise<void>
+  onSetLaunchAtLogin: (enabled: boolean) => Promise<void>
   onSelectProfile: (name: string) => Promise<void>
   section: Section
   selectedProfile: string
@@ -985,33 +987,56 @@ function SectionContent({
   }
 
   return (
-    <Surface>
-      <div className="grid gap-5 p-5 md:grid-cols-[1fr_auto] md:items-center">
-        <div>
-          <div className="flex items-center gap-2">
-            <IconBrandWindows className="size-5 text-(--ui-accent)" />
-            <h3 className="text-base font-semibold">{snapshot.version?.product.name || 'Hermes Launcher'}</h3>
-          </div>
-          <p className="mt-2 text-sm leading-6 text-(--ui-text-secondary)">
-            Windows-native local AI workstation built on the official Hermes Agent Desktop and pinned llama.cpp runtime.
-          </p>
-          <dl className="mt-4 grid gap-3 text-xs sm:grid-cols-2">
-            <div>
-              <dt className="text-(--ui-text-tertiary)">Version</dt>
-              <dd className="mt-1 font-mono">{snapshot.version?.product.version || 'Unavailable'}</dd>
-            </div>
-            <div>
-              <dt className="text-(--ui-text-tertiary)">Project root</dt>
-              <dd className="mt-1 truncate font-mono">{snapshot.root}</dd>
+    <div className="grid gap-4">
+      <Surface>
+        <div className="grid gap-5 p-5 md:grid-cols-[1fr_auto] md:items-center">
+          <div>
+            <div className="flex items-center gap-2">
+              <IconBrandWindows className="size-5 text-(--ui-accent)" />
+              <h3 className="text-base font-semibold">{snapshot.version?.product.name || 'Hermes Launcher'}</h3>
             </div>
-          </dl>
+            <p className="mt-2 text-sm leading-6 text-(--ui-text-secondary)">
+              Windows-native local AI workstation built on the official Hermes Agent Desktop and pinned llama.cpp runtime.
+            </p>
+            <dl className="mt-4 grid gap-3 text-xs sm:grid-cols-2">
+              <div>
+                <dt className="text-(--ui-text-tertiary)">Version</dt>
+                <dd className="mt-1 font-mono">{snapshot.version?.product.version || 'Unavailable'}</dd>
+              </div>
+              <div>
+                <dt className="text-(--ui-text-tertiary)">Project root</dt>
+                <dd className="mt-1 truncate font-mono">{snapshot.root}</dd>
+              </div>
+            </dl>
+          </div>
+          <Button className="gap-2" onClick={() => void window.hermesDesktop.localWorkstation.openRoot()} variant="outline">
+            <IconFolder className="size-4" />
+            Open project
+          </Button>
         </div>
-        <Button className="gap-2" onClick={() => void window.hermesDesktop.localWorkstation.openRoot()} variant="outline">
-          <IconFolder className="size-4" />
-          Open project
-        </Button>
-      </div>
-    </Surface>
+      </Surface>
+      <Surface title="Windows sign-in">
+        <div className="grid gap-4 p-5 md:grid-cols-[1fr_auto] md:items-center">
+          <div>
+            <h3 className="text-sm font-semibold">Launch Hermes at sign-in</h3>
+            <p className="mt-1 text-xs leading-5 text-(--ui-text-secondary)">
+              Registers this executable for the current Windows user only. No elevation or scheduled task is used.
+            </p>
+            <p className="mt-2 truncate font-mono text-[0.6875rem] text-(--ui-text-tertiary)">
+              {snapshot.startup.executable}
+            </p>
+          </div>
+          <Button
+            aria-label={`${snapshot.startup.enabled ? 'Disable' : 'Enable'} launch at sign-in`}
+            disabled={!snapshot.startup.available}
+            onClick={() => void onSetLaunchAtLogin(!snapshot.startup.enabled)}
+            variant={snapshot.startup.enabled ? 'default' : 'outline'}
+          >
+            {snapshot.startup.enabled ? 'Enabled' : 'Enable'}
+          </Button>
+        </div>
+      </Surface>
+    </div>
   )
 }
 
@@ -1089,6 +1114,18 @@ export function LocalWorkstationView() {
     [refresh]
   )
 
+  const setLaunchAtLogin = useCallback(
+    async (enabled: boolean) => {
+      try {
+        await window.hermesDesktop.localWorkstation.loginItem.set(enabled)
+        await refresh()
+      } catch (nextError) {
+        setError(nextError instanceof Error ? nextError.message : String(nextError))
+      }
+    },
+    [refresh]
+  )
+
   const copy = SECTION_COPY[section]
 
   if (!snapshot) {
@@ -1158,6 +1195,7 @@ export function LocalWorkstationView() {
             onRun={action => void run(action)}
             onSaveProfile={saveProfile}
             onSelectProfile={selectProfile}
+            onSetLaunchAtLogin={setLaunchAtLogin}
             section={section}
             selectedProfile={profileView}
             snapshot={snapshot}
diff --git a/apps/desktop/src/app/local-workstation/types.ts b/apps/desktop/src/app/local-workstation/types.ts
index 543a6d0b3..7e1ced1f0 100644
--- a/apps/desktop/src/app/local-workstation/types.ts
+++ b/apps/desktop/src/app/local-workstation/types.ts
@@ -22,6 +22,12 @@ export interface LocalActionTask {
   status: 'failed' | 'running' | 'succeeded'
 }
 
+export interface LocalLoginItemStatus {
+  available: boolean
+  enabled: boolean
+  executable: string
+}
+
 export interface LocalInferenceProfile {
   batch: {
     logical: number
@@ -109,6 +115,7 @@ export interface LocalWorkstationSnapshot {
     memoryFiles: number
     stateDatabaseBytes: number
   }
+  startup: LocalLoginItemStatus
   taskLedger: string
   runtime: {
     controllerAlive: boolean
diff --git a/apps/desktop/src/global.d.ts b/apps/desktop/src/global.d.ts
index 9860c76fa..d24cb6430 100644
--- a/apps/desktop/src/global.d.ts
+++ b/apps/desktop/src/global.d.ts
@@ -126,6 +126,10 @@ declare global {
       }
       localWorkstation: {
         actionStatus: (taskId: string) => Promise<LocalActionTask>
+        loginItem: {
+          get: () => Promise<LocalLoginItemStatus>
+          set: (enabled: boolean) => Promise<LocalLoginItemStatus>
+        }
         logs: (name: LocalLog, lines?: number) => Promise<LocalLogResult>
         openDashboard: () => Promise<{ url: string }>
         openRoot: () => Promise<{ error: string; ok: boolean }>
