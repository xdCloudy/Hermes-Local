From ba39a47eb1d38914229956b0521690eb7685db3f Mon Sep 17 00:00:00 2001
From: xdCloudy <52116030+xdCloudy@users.noreply.github.com>
Date: Thu, 30 Jul 2026 17:18:26 +0000
Subject: [PATCH] refactor(desktop): remove redirect-only Tools page

---
 .../desktop/e2e/hermes-local-portable.spec.ts |  2 ++
 apps/desktop/src/app/chat/sidebar/index.tsx   |  7 ----
 apps/desktop/src/app/contrib/surfaces.tsx     |  4 +--
 .../src/app/local-workstation/index.tsx       | 32 +------------------
 apps/desktop/src/app/routes.test.ts           | 18 ++++++++++-
 apps/desktop/src/app/routes.ts                |  1 +
 6 files changed, 23 insertions(+), 41 deletions(-)

diff --git a/apps/desktop/e2e/hermes-local-portable.spec.ts b/apps/desktop/e2e/hermes-local-portable.spec.ts
index 1d93496..e7ca56e 100644
--- a/apps/desktop/e2e/hermes-local-portable.spec.ts
+++ b/apps/desktop/e2e/hermes-local-portable.spec.ts
@@ -135,6 +135,8 @@ test.describe('Hermes Local portable workstation', () => {
 
       const workstationNav = page.locator('[data-sidebar="menu"]').first()
 
+      await expect(workstationNav.getByRole('button', { exact: true, name: 'Tools' })).toHaveCount(0)
+
       await workstationNav.getByRole('button', { name: 'Home', exact: true }).click()
       await expect(page.getByRole('heading', { name: 'Local AI workstation' })).toBeVisible({ timeout: 60_000 })
       await expect(page.getByText('Ready for local inference')).toBeVisible()
diff --git a/apps/desktop/src/app/chat/sidebar/index.tsx b/apps/desktop/src/app/chat/sidebar/index.tsx
index 12be1e2..1ff370c 100644
--- a/apps/desktop/src/app/chat/sidebar/index.tsx
+++ b/apps/desktop/src/app/chat/sidebar/index.tsx
@@ -116,7 +116,6 @@ import {
   type SidebarNavContribution,
   SKILLS_ROUTE,
   TASKS_ROUTE,
-  TOOLS_ROUTE,
   TUI_ROUTE
 } from '../../routes'
 import type { SidebarNavItem } from '../../types'
@@ -203,12 +202,6 @@ const SIDEBAR_NAV: SidebarNavItem[] = [
     icon: props => <Codicon name="settings-gear" {...props} />,
     route: LOCAL_PROFILES_ROUTE
   },
-  {
-    id: 'tools',
-    label: '',
-    icon: props => <Codicon name="tools" {...props} />,
-    route: TOOLS_ROUTE
-  },
   {
     id: 'memory',
     label: '',
diff --git a/apps/desktop/src/app/contrib/surfaces.tsx b/apps/desktop/src/app/contrib/surfaces.tsx
index 0e455dc..d040a27 100644
--- a/apps/desktop/src/app/contrib/surfaces.tsx
+++ b/apps/desktop/src/app/contrib/surfaces.tsx
@@ -19,7 +19,7 @@ import { $freshDraftReady, $gatewayState } from '@/store/session'
 import { ChatView } from '../chat'
 import { ChatSidebar } from '../chat/sidebar'
 import { TerminalPaneChrome } from '../right-sidebar/terminal/chrome'
-import { contributedRoutes, NEW_CHAT_ROUTE, ROUTES_AREA, sessionRoute } from '../routes'
+import { contributedRoutes, LEGACY_TOOLS_REDIRECT_ROUTE, NEW_CHAT_ROUTE, ROUTES_AREA, sessionRoute } from '../routes'
 import { useStatusSnapshot } from '../shell/hooks/use-status-snapshot'
 import { useStatusbarItems } from '../shell/hooks/use-statusbar-items'
 import { ModelMenuPanel } from '../shell/model-menu-panel'
@@ -176,7 +176,7 @@ export const ChatRoutesSurface = memo(function ChatRoutesSurface({
       <Route element={page(<LocalWorkstationView />)} path="services" />
       <Route element={page(<LocalWorkstationView />)} path="models" />
       <Route element={page(<LocalWorkstationView />)} path="local-profiles" />
-      <Route element={page(<LocalWorkstationView />)} path="tools" />
+      <Route element={<Navigate replace to={LEGACY_TOOLS_REDIRECT_ROUTE} />} path="tools" />
       <Route element={page(<LocalWorkstationView />)} path="memory" />
       <Route element={page(<LocalWorkstationView />)} path="sessions" />
       <Route element={page(<LocalWorkstationView />)} path="projects" />
diff --git a/apps/desktop/src/app/local-workstation/index.tsx b/apps/desktop/src/app/local-workstation/index.tsx
index 5da675f..b47a7a7 100644
--- a/apps/desktop/src/app/local-workstation/index.tsx
+++ b/apps/desktop/src/app/local-workstation/index.tsx
@@ -22,7 +22,6 @@ import IconServer2 from '@tabler/icons-react/dist/esm/icons/IconServer2.mjs'
 import IconShieldCheck from '@tabler/icons-react/dist/esm/icons/IconShieldCheck.mjs'
 import IconSquare from '@tabler/icons-react/dist/esm/icons/IconSquare.mjs'
 import IconTerminal2 from '@tabler/icons-react/dist/esm/icons/IconTerminal2.mjs'
-import IconTool from '@tabler/icons-react/dist/esm/icons/IconTool.mjs'
 import IconTrash from '@tabler/icons-react/dist/esm/icons/IconTrash.mjs'
 import IconX from '@tabler/icons-react/dist/esm/icons/IconX.mjs'
 import { type ComponentType, type ReactNode, useCallback, useEffect, useState } from 'react'
@@ -56,8 +55,7 @@ const SECTION_COPY = {
   security: ['Security', 'Loopback trust boundaries, audits and remediation evidence.'],
   sessions: ['Sessions', 'Resume and manage persistent local Hermes conversations.'],
   services: ['Services', 'Structured health, process ownership and lifecycle controls.'],
-  tasks: ['Tasks', 'The live implementation and release ledger.'],
-  tools: ['Tools', 'Local terminal, browser, file and agent capability entry points.']
+  tasks: ['Tasks', 'The live implementation and release ledger.']
 } as const
 
 type Section = keyof typeof SECTION_COPY
@@ -1432,34 +1430,6 @@ function SectionContent({
     )
   }
 
-  if (section === 'tools') {
-    return (
-      <div className="grid gap-4 md:grid-cols-3">
-        {[
-          [
-            'Terminal and process tools',
-            'Run local commands with exact working-directory visibility.',
-            IconTerminal2,
-            '/tui'
-          ],
-          ['Capabilities and skills', 'Inspect installed skills and local toolsets.', IconTool, '/skills'],
-          ['Desktop chat', 'Use file, browser, memory and agent tools through Hermes.', IconRocket, '/']
-        ].map(([label, detail, Icon, path]) => (
-          <button
-            className="rounded-xl border border-(--ui-stroke-secondary) bg-(--ui-panel-surface-background) p-5 text-left hover:border-(--ui-accent)/40"
-            key={String(label)}
-            onClick={() => onNavigate(path as string)}
-            type="button"
-          >
-            <Icon className="size-5 text-(--ui-accent)" stroke={1.7} />
-            <h3 className="mt-3 text-sm font-semibold">{label as string}</h3>
-            <p className="mt-1 text-xs leading-5 text-(--ui-text-tertiary)">{detail as string}</p>
-          </button>
-        ))}
-      </div>
-    )
-  }
-
   return (
     <div className="grid gap-4">
       <Surface>
diff --git a/apps/desktop/src/app/routes.test.ts b/apps/desktop/src/app/routes.test.ts
index 3c8933d..3c3c007 100644
--- a/apps/desktop/src/app/routes.test.ts
+++ b/apps/desktop/src/app/routes.test.ts
@@ -1,6 +1,15 @@
 import { describe, expect, it } from 'vitest'
 
-import { NEW_CHAT_ROUTE, primaryRouteSelectedSessionId, sessionRoute, SETTINGS_ROUTE } from './routes'
+import {
+  appViewForPath,
+  LEGACY_TOOLS_REDIRECT_ROUTE,
+  NEW_CHAT_ROUTE,
+  primaryRouteSelectedSessionId,
+  sessionRoute,
+  SETTINGS_ROUTE,
+  SKILLS_ROUTE,
+  TOOLS_ROUTE
+} from './routes'
 
 const SESS_A = 'sess-a'
 const SESS_B = 'sess-b'
@@ -28,3 +37,10 @@ describe('primaryRouteSelectedSessionId', () => {
     expect(primaryRouteSelectedSessionId(SETTINGS_ROUTE, null)).toBeNull()
   })
 })
+
+describe('legacy Tools route', () => {
+  it('remains reserved and redirects to the existing Skills surface', () => {
+    expect(appViewForPath(TOOLS_ROUTE)).toBe('workstation')
+    expect(LEGACY_TOOLS_REDIRECT_ROUTE).toBe(SKILLS_ROUTE)
+  })
+})
diff --git a/apps/desktop/src/app/routes.ts b/apps/desktop/src/app/routes.ts
index 2c1f45a..46875a0 100644
--- a/apps/desktop/src/app/routes.ts
+++ b/apps/desktop/src/app/routes.ts
@@ -33,6 +33,7 @@ export const BENCHMARKS_ROUTE = '/benchmarks'
 export const SECURITY_ROUTE = '/security'
 export const LOGS_ROUTE = '/logs'
 export const ABOUT_ROUTE = '/about'
+export const LEGACY_TOOLS_REDIRECT_ROUTE = SKILLS_ROUTE
 
 export type AppView =
   | 'agents'
-- 
2.54.0

