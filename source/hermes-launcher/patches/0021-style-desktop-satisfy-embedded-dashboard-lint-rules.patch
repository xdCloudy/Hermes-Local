From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: OpenAI <noreply@openai.com>
Date: Fri, 31 Jul 2026 02:09:50 +0000
Subject: [PATCH 21/36] style(desktop): satisfy embedded dashboard lint rules

---
 apps/desktop/electron/hermes-local-control.ts          | 10 +++++-----
 .../electron/hermes-local-dashboard-view.test.ts       |  5 +++++
 apps/desktop/electron/hermes-local-dashboard-view.ts   | 10 +++++++++-
 apps/desktop/electron/main.ts                          |  2 ++
 apps/desktop/src/app/local-workstation/index.tsx       |  3 +++
 5 files changed, 24 insertions(+), 6 deletions(-)

diff --git a/apps/desktop/electron/hermes-local-control.ts b/apps/desktop/electron/hermes-local-control.ts
index d8c11d545f5b77b92b12cbc9606395dc859f4002..5bf3705fec64f7d923bf8306738925fd04f5e0a2 100644
--- a/apps/desktop/electron/hermes-local-control.ts
+++ b/apps/desktop/electron/hermes-local-control.ts
@@ -8,11 +8,11 @@ import { promisify } from 'node:util'
 import { app, ipcMain, shell } from 'electron'
 
 import {
-  normalizeHermesLocalDashboardUrl,
   type HermesLocalDashboardBounds,
-  type HermesLocalDashboardViewController
+  type HermesLocalDashboardViewController,
+  normalizeHermesLocalDashboardUrl
 } from './hermes-local-dashboard-view'
-
+import { readGatewayStatus } from './hermes-local-gateway-status'
 import {
   deleteProfile,
   readLocalConfiguration,
@@ -26,8 +26,6 @@ import {
   validProfileName
 } from './hermes-local-settings'
 
-import { readGatewayStatus } from './hermes-local-gateway-status'
-
 const execFileAsync = promisify(execFile)
 const MAX_TASK_OUTPUT = 128 * 1024
 const MAX_COMPLETED_TASKS = 50
@@ -699,9 +697,11 @@ export function configureHermesLocalDesktopEnvironment(): boolean {
 
 function dashboardConfiguration() {
   const configuration = readLocalConfiguration(localRoot(), os.cpus().length)
+
   const host = configuration.network.host.includes(':')
     ? `[${configuration.network.host.replace(/^\[|\]$/g, '')}]`
     : configuration.network.host
+
   const url = normalizeHermesLocalDashboardUrl(`http://${host}:${configuration.network.hermesPort}`)
   const token = String(process.env.HERMES_DESKTOP_REMOTE_TOKEN || '').trim()
 
diff --git a/apps/desktop/electron/hermes-local-dashboard-view.test.ts b/apps/desktop/electron/hermes-local-dashboard-view.test.ts
index 5cbbb0595eaff6fce608b8880709a71e71d28a0c..98551f7b102a83094533849209c930c4ab96fcca 100644
--- a/apps/desktop/electron/hermes-local-dashboard-view.test.ts
+++ b/apps/desktop/electron/hermes-local-dashboard-view.test.ts
@@ -95,6 +95,7 @@ function harness() {
   const views: FakeView[] = []
   const childViews = new Set<FakeView>()
   const renderer = {}
+
   const window = {
     contentView: {
       addChildView: vi.fn((view: FakeView) => childViews.add(view)),
@@ -104,9 +105,11 @@ function harness() {
     isDestroyed: () => false,
     webContents: renderer
   }
+
   const partition = fakeSession()
   const openExternal = vi.fn(async () => undefined)
   const timers: Array<() => void> = []
+
   const controller = createHermesLocalDashboardViewController({
     clearTimeout: vi.fn(),
     createView: () => {
@@ -175,12 +178,14 @@ describe('Hermes Local embedded dashboard policy', () => {
         resolve
       )
     )
+
     const externalResult = await new Promise<any>(resolve =>
       partition.handlers.beforeSendHeaders(
         { requestHeaders: { Accept: 'text/html' }, url: 'https://example.com/' },
         resolve
       )
     )
+
     const blocked = await new Promise<any>(resolve =>
       partition.handlers.beforeRequest({ url: 'https://example.com/' }, resolve)
     )
diff --git a/apps/desktop/electron/hermes-local-dashboard-view.ts b/apps/desktop/electron/hermes-local-dashboard-view.ts
index ddc518f72d1e2f0cd48de575e9fd011deba0003b..be14a0c8a5579026dbecd265505e3430a0d9a0b8 100644
--- a/apps/desktop/electron/hermes-local-dashboard-view.ts
+++ b/apps/desktop/electron/hermes-local-dashboard-view.ts
@@ -1,4 +1,12 @@
-import { app, session, shell, WebContentsView, type BrowserWindow, type Rectangle, type Session } from 'electron'
+import {
+  app,
+  type BrowserWindow,
+  type Rectangle,
+  session,
+  type Session,
+  shell,
+  WebContentsView
+} from 'electron'
 
 const DASHBOARD_PARTITION = 'hermes:local-dashboard'
 const RETRY_DELAYS_MS = [750, 1500, 3000, 6000, 10_000]
diff --git a/apps/desktop/electron/main.ts b/apps/desktop/electron/main.ts
index 4a158ca594ebb860f041468a72d2fbcadb0a20c1..10f57d6961d42e6b01131df46d50ebc48f5b3669 100644
--- a/apps/desktop/electron/main.ts
+++ b/apps/desktop/electron/main.ts
@@ -1056,6 +1056,7 @@ function registerMediaProtocol() {
 }
 
 let mainWindow = null
+
 const hermesLocalDashboardView = createHermesLocalDashboardViewController({
   emitState: state => {
     if (mainWindow && !mainWindow.isDestroyed()) {
@@ -1064,6 +1065,7 @@ const hermesLocalDashboardView = createHermesLocalDashboardViewController({
   },
   getWindow: () => mainWindow
 })
+
 const backendConnectionState = createBackendConnectionState<ReturnType<typeof spawn>, any>()
 const remoteLiveness = new RemoteLivenessTracker()
 const remoteRevalidation = new RemoteRevalidationCoordinator()
diff --git a/apps/desktop/src/app/local-workstation/index.tsx b/apps/desktop/src/app/local-workstation/index.tsx
index a4187b8215fd549b3b8b17ef31a6de185e2a923e..9a35816e8e07fd343e27036e8fba57a26f358f4e 100644
--- a/apps/desktop/src/app/local-workstation/index.tsx
+++ b/apps/desktop/src/app/local-workstation/index.tsx
@@ -1013,6 +1013,7 @@ function HomeContent({
 function DashboardContent({ snapshot }: { snapshot: LocalWorkstationSnapshot }) {
   const dashboard = window.hermesDesktop.localWorkstation.dashboard
   const hostRef = useRef<HTMLDivElement>(null)
+
   const [state, setState] = useState<HermesLocalDashboardState>({
     canRetry: true,
     message: dashboard
@@ -1048,6 +1049,7 @@ function DashboardContent({ snapshot }: { snapshot: LocalWorkstationSnapshot })
 
     let active = true
     let frame = 0
+
     const unsubscribe = dashboard.onState(next => {
       if (active) {
         setState(next)
@@ -1108,6 +1110,7 @@ function DashboardContent({ snapshot }: { snapshot: LocalWorkstationSnapshot })
     ready: ['Dashboard connected', IconCheck],
     restarting: ['Dashboard restarting', IconRefresh]
   } as const
+
   const [phaseLabel, PhaseIcon] = phaseCopy[state.phase]
   const loading = state.phase === 'loading' || state.phase === 'restarting'
 
